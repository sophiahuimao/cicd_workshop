import { NameWinnerPipe } from './name-winner.pipe';

describe('NameWinnerPipe', () => {
  it('create an instance', () => {
    const pipe = new NameWinnerPipe();
    expect(pipe).toBeTruthy();
  });
});
