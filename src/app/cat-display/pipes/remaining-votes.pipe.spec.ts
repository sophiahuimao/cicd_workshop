import { RemainingVotesPipe } from './remaining-votes.pipe';

describe('RemainingVotesPipe', () => {
  it('create an instance', () => {
    const pipe = new RemainingVotesPipe();
    expect(pipe).toBeTruthy();
  });
});
